apiVersion: v1
kind: Service
metadata:
  name: payment-mock
  labels:
    app: payment-mock
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: payment-mock
  ports:
    - port: 8100
      targetPort: 8100
      name: http